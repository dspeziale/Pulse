# Dashboard SERVER — dipendenze di runtime (versioni pinnate a range compatibili).
# Il pacchetto condiviso pulse-fe-common va installato a parte (vedi Dockerfile:
# `pip install ./frontend_common`), essendo sorgente comune ai due frontend.
Flask>=3.0,<4
httpx>=0.27,<1
gunicorn>=21,<23
# Report PDF del Compendio sistema (puro Python, nessuna libreria di sistema:
# vedi README §Compendio / Report PDF per la scelta fpdf2 vs WeasyPrint).
fpdf2>=2.8,<3
